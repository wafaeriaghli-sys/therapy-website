## Packages
framer-motion | Smooth page transitions and scroll animations
@hookform/resolvers | Form validation with Zod

## Notes
Static images are imported via Vite's @assets alias.
The design uses custom Google Fonts (Playfair Display and DM Sans).
The aesthetic relies heavily on the generated CSS custom properties to maintain a warm, sage green, and terracotta palette.
